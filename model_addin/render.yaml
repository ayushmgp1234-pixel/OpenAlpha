services:
  - type: web
    name: model-addin-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --workers 2 --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    healthCheckPath: /health
